# Screenshots

## Act 3 override (`act3-override.png`)

Capture after running Act 3 in the browser:

1. Open https://webmcp-playground-three.vercel.app/
2. Run Act 3 (core IP + pin Build via agent or human controls)
3. Frame: override banner + option cards + **Liability ledger** populated in the right rail
4. Save as `act3-override.png` in this folder
5. Uncomment the image line in the root `README.md` hero
